import csv
import sys

with open('insurance.csv', mode='r') as infile:
    reader = csv.reader(infile)
    with open('insurance 2.csv', mode='w') as outfile:
        writer = csv.writer(outfile)
        mydict = {rows[0]:rows[1] for rows in reader}
       
 print(mydict)
